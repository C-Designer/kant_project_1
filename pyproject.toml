[project]
name = "kant-project-1"
version = "0.1.0"
description = "Reproducible local LLM Python bug-fix comparison"
requires-python = ">=3.12,<3.13"
dependencies = [
    "openai>=3.14.1",
    "pytest==8.3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
